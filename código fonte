# Simulação simples da adição binária de dois valores

# Memória simulada
memoria = {
    "NUM1": 6,      # Primeiro número
    "NUM2": 3,      # Segundo número
    "RESULT": 0     # Onde o resultado será armazenado
}

# Acumulador
AC = 0  # Registrador acumulador (como no Neander)

# Instruções simuladas
AC = memoria["NUM1"]     # LDA NUM1
AC += memoria["NUM2"]    # ADD NUM2
memoria["RESULT"] = AC   # STA RESULT

# Resultado
print(f"O resultado de {memoria['NUM1']} + {memoria['NUM2']} é: {memoria['RESULT']}")
